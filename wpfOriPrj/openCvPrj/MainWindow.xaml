<Window x:Class="openCvPrj.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:openCvPrj"
        mc:Ignorable="d"
        Title="MainWindow" Height="474" Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.9*"/>
            <RowDefinition Height="0.1*"/>
        </Grid.RowDefinitions>

        <!-- 원본 이미지 -->
        <Image x:Name="OriginalImage" Grid.Column="0" Stretch="Uniform" Margin="5"/>

        <!-- 변환된 이미지 -->
        <Image x:Name="ProcessedImage" Grid.Column="1" Stretch="Uniform" Margin="5"/>

        <Button Content="이미지 변환"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Width="120"
                Height="30"
                Click="Button_Click" Grid.Row="1"/>
    </Grid>
</Window>
